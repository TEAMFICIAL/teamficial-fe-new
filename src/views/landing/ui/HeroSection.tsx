import { LandingContainer } from "@/views/landing/ui/LandingContainer";

export function HeroSection() {
  return (
    <LandingContainer className="gradient-bg-01">
      <div className="flex flex-col items-center justify-center gap-3 py-20">
        상단 히어로섹션입니다.
      </div>
    </LandingContainer>
  );
}
